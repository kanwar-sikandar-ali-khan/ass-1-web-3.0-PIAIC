//p32

// type Users = {
//     name: string;
//     role: string;
// };

// const current_users: Array<Users> = [{ name: "kanwar", role: "user" }, { name: "sikandar", role: "admin" },
// { name: "ali", role: "user" }, { name: "khan", role: "user" }];

// const new_users: Array<Users> = [{ name: "kANwar", role: "user" }, { name: "saad", role: "admin" },
// { name: "Ali", role: "user" }, { name: "khAn", role: "user" }];

// for (var i = 0; i < current_users.length; i++) {
//     const userExists = new_users.some((user) => user.name.toLowerCase() === current_users[i].name.toLowerCase() );

//     if (userExists) {
//         console.log("need to enter a new username");
//     } else {
//         console.log("username is available");
//     }
// }