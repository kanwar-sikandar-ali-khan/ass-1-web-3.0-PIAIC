// p31

// type Users = {
//     name: string;
//     role: string;
// };

// const users: Array<Users> = [{ name: "Ali", role: "user" }, { name: "Ali", role: "admin" }, { name: "atif", role: "user" }];

// for (let i = users.length - 1; i >= 0; i--) {
//     users.pop();
//   }
// console.log(users.length !== 0? "we have users" :"We need to find some users!")